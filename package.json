{
  "name": "waytohome",
  "version": "0.1.0",
  "description": "The main idea of project – planning routes and get a notification",
  "main": "index.js",
  "scripts": {
    "start": "webpack -d --watch --config webpack.config.js",
    "build": "webpack --config webpack.config.js",
    "test": "nyc --reporter=lcov --reporter=text webpack --config webpack-test.config.js",
    "lint": "eslint --config .eslintrc.js way_to_home/static/src/;exit 0",
    "build:dev": "webpack --display-error-details --config webpack.config.js --progress --color",
    "build:prod": "webpack --env.prod --config webpack.config.js --progress --color"
  },
  "devDependencies": {
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "react": "16.3.0",
    "react-dom": "16.3.0"
  }
}
